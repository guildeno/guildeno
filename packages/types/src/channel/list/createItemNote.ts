/** Type definition of how a create list item note request should look like. */
export type CreateListItemNote = {
    /**
     * Content of this note.
     * In markdown format.
     */
    content: string;
};
