/** Type definition of how an update list item note request should look like. */
export type UpdateListItemNote = {
    /**
     * Content of this note.
     * In markdown format.
     */
    content: string;
};
