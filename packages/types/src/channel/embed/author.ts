/** Type definition of an embed author. */
export type EmbedAuthor = {
    /** Name of the author. */
    name?: string;
    /** URL to linkify the author's `name` field. */
    url?: string;
    /** URL of a small image to display to the left of the author's `name`. */
    icon_url?: string;
};
